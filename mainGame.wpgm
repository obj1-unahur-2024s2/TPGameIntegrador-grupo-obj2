import wollok.game.*
import juego.*

program JuegoPrincipal {
	// game.title("Pepita")
	// game.height(10)
	// game.width(10)

	// add assets in asset folder, for example, for the background
  // game.boardGround("fondo2.jpg")

	//

	// game.showAttributes(pepita) //Debug
	juego.iniciar()
	game.start()
}
